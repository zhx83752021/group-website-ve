---
description: 代码修改完成后自动提交并推送 Git，无需再询问用户
alwaysApply: true
---

# Git 提交与推送

在本仓库中完成**任意代码或配置修改**后，**默认必须**执行（除非用户明确说不要提交）：

1. `git add` 相关文件（或 `git add -A` 当变更即本次任务全部内容时）
2. `git commit -m "..."`：使用清晰的中文或英文说明**改了什么、为什么**
3. `git push origin main`（或当前分支）

**不要**在每次任务结束询问「要不要提交」；直接提交并推送。若 `git push` 失败，根据错误信息重试或告知用户需要处理网络/权限。

若工作区无实质变更，可跳过提交。
